<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lyndon's Portfolio</title>
  <style>
    /* ------------------
       Theme variables
    ------------------ */
    :root {
      --bg-dark: url("sunset.jpg");
      --bg-light: url("sunrise.jpg");
      --text-dark: #e6eef3;
      --text-light: #1a1a1a;
      --muted-dark: #9aa7b2;
      --muted-light: #555;
      --card-dark: rgba(0,0,0,0.55);
      --card-light: rgba(255,255,255,0.85);
      --accent: #ffcc70;
    }

    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: var(--bg-dark) no-repeat center center fixed;
      background-size: cover;
      color: var(--text-dark);
      transition: background 1s ease, color 0.5s ease;
      scroll-behavior: smooth;
    }

    body.light {
      background: var(--bg-light) no-repeat center center fixed;
      background-size: cover;
      color: var(--text-light);
    }

    header, nav, section, footer {
      background: var(--card-dark);
      border-radius: 20px;
      margin: 20px auto;
      padding: 20px;
      width: min(90%, 1000px);
      backdrop-filter: blur(8px);
      transition: background 1s ease;
    }

    body.light header,
    body.light nav,
    body.light section,
    body.light footer {
      background: var(--card-light);
    }

    /* ------------------
       Header Profile
    ------------------ */
    header .profile-box {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 10px;
      animation: fadeIn 1s ease-in;
    }

    header img {
      width: 130px;
      height: 130px;
      border-radius: 50%;
      object-fit: cover;
      box-shadow: 0 0 20px rgba(255,255,255,0.3);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      cursor: pointer;
    }

    header img:hover {
      transform: scale(1.08);
      box-shadow: 0 0 30px rgba(255,255,255,0.6);
    }

    h1 {
      font-size: 1.8rem;
      margin: 10px 0;
    }

    .btn {
      display: inline-block;
      margin-top: 10px;
      padding: 10px 20px;
      border-radius: 30px;
      background: var(--accent);
      color: #000;
      font-weight: bold;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn:hover {
      background: #ffe08a;
      transform: translateY(-3px);
      box-shadow: 0 5px 15px rgba(255,255,255,0.3);
    }

    /* ------------------
       Navigation
    ------------------ */
    nav {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 20px;
      flex-wrap: wrap;
      animation: fadeIn 1.2s ease;
    }

    nav a {
      text-decoration: none;
      color: inherit;
      font-weight: 600;
      transition: color 0.3s, text-shadow 0.3s;
    }

    nav a:hover {
      color: var(--accent);
      text-shadow: 0 0 10px var(--accent);
    }

    .theme-toggle {
      cursor: pointer;
      border: none;
      padding: 8px 16px;
      border-radius: 30px;
      font-size: 14px;
      font-weight: 600;
      background: rgba(255,255,255,0.15);
      color: inherit;
      transition: all 0.3s ease;
    }

    .theme-toggle:hover {
      background: rgba(255,255,255,0.3);
      transform: scale(1.05);
    }

    /* ------------------
       Sections
    ------------------ */
    section {
      text-align: center;
      animation: fadeUp 1s ease;
    }

    section h2 {
      margin-bottom: 15px;
      font-size: 1.5rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--accent);
    }

    .image-grid {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
    }

    .image-box {
      text-align: center;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }

    .image-box img {
      width: 150px;
      height: 150px;
      object-fit: cover;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0,0,0,0.4);
    }

    .image-box:hover {
      transform: translateY(-10px);
    }

    footer {
      text-align: center;
      color: var(--muted-dark);
      font-size: 14px;
      margin-bottom: 20px;
      animation: fadeIn 1.5s ease;
    }

    body.light footer {
      color: var(--muted-light);
    }

    /* ------------------
       Animations
    ------------------ */
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <!-- HEADER -->
  <header id="home">
    <div class="profile-box">
      <img src="profile.jpg" alt="Profile Picture">
      <h1>Lyndon Jay V. Aron</h1>
      <p>Born: September 6, 2004 | Age: 21 | Height: 5’8”</p>
      <p>Basketball Lover 🏀 | Adaptable | Growth-driven</p>
      <a href="Lyndon_Aron_CV.pdf" download class="btn">📄 Download CV</a>
    </div>
  </header>

  <!-- NAVIGATION -->
  <nav>
    <a href="#home">Home</a>
    <a href="#about">About</a>
    <a href="#favorites">Favorites</a>
    <a href="#contact">Contact</a>
    <button class="theme-toggle" id="themeBtn">🌅 Light Mode</button>
  </nav>

  <!-- ABOUT -->
  <section id="about">
    <h2>About Me</h2>
    <p>Hello! I’m Lyndon, a passionate basketball player and aspiring creator. I believe in continuous learning, teamwork, and staying motivated to reach my goals. I’m currently exploring opportunities to apply my adaptability and creativity in different fields.</p>
  </section>

  <!-- FAVORITES -->
  <section id="favorites">
    <h2>My Top 3 Favorites</h2>

    <h3>🎵 Song Artists</h3>
    <div class="image-grid">
      <div class="image-box"><img src="artist1.jpg" alt="Artist 1"><p>Artist Name</p></div>
      <div class="image-box"><img src="artist2.jpg" alt="Artist 2"><p>Artist Name</p></div>
      <div class="image-box"><img src="artist3.jpg" alt="Artist 3"><p>Artist Name</p></div>
    </div>

    <h3>🎌 Anime</h3>
    <div class="image-grid">
      <div class="image-box"><img src="anime1.jpg" alt="Anime 1"><p>Anime Title</p></div>
      <div class="image-box"><img src="anime2.jpg" alt="Anime 2"><p>Anime Title</p></div>
      <div class="image-box"><img src="anime3.jpg" alt="Anime 3"><p>Anime Title</p></div>
    </div>

    <h3>🎥 Attack on Titan Movies</h3>
    <div class="image-grid">
      <div class="image-box"><img src="aot1.jpg" alt="AOT Movie 1"><p>Movie Title</p></div>
      <div class="image-box"><img src="aot2.jpg" alt="AOT Movie 2"><p>Movie Title</p></div>
      <div class="image-box"><img src="aot3.jpg" alt="AOT Movie 3"><p>Movie Title</p></div>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact">
    <h2>Contact Me</h2>
    <p>📧 Email: <a href="mailto:your-email@gmail.com">your-email@gmail.com</a></p>
    <p>📷 Instagram: <a href="#">@yourusername</a></p>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>&copy; 2025 Lyndon Jay V. Aron | All Rights Reserved</p>
  </footer>

  <!-- SCRIPT -->
  <script>
    const themeBtn = document.getElementById("themeBtn");
    themeBtn.addEventListener("click", () => {
      document.body.classList.toggle("light");
      themeBtn.textContent = document.body.classList.contains("light") 
        ? "🌆 Dark Mode" 
        : "🌅 Light Mode";
    });
  </script>
</body>
</html>
